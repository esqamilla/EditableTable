# EditableTable
Таблица с возможностью добавления папок и файлов в них с подсчетом общей стоимости по каждой папке

### Запуск проекта:
1. В директорию "/frontend" устанавливаем node модули командой `yarn install`
2. В той же директории пишем команду `yarn start`
